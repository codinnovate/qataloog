import { Input } from "./ui/input";

export default function SearchBar(){
    return (
            <Input
             placeholder="Search"
             className="placeholder:text-grey"
             />
        </div>
    )
}